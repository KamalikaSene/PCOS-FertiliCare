import { Injectable } from '@nestjs/common';

@Injectable();
export class DoctorsService {};

// service file to handle all the methods for all the http requests being made to the backend which are then handled by the particular route in the controller file
